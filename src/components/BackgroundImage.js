import styled from 'styled-components';

const Background = styled.div`
  width: 100%;
  height: 100vh;
  background: radial-gradient(100% 100% at 50% 100%, rgba(0, 98, 255, 0.35) 0%, rgba(0, 98, 255, 0) 100%);
`;

export default Background;
